upstream django {
    server web:8000;
}

server {
    listen 80;
    server_name _;
    client_max_body_size 10M;

    # Certbot challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location /health/ {
        proxy_pass http://django;
        access_log off;
    }

    location /static/ {
        alias /app/staticfiles/;
        expires 30d;
    }

    location / {
        # If certs exist, redirect to HTTPS
        # Otherwise serve directly
        proxy_pass http://django;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 30s;
        proxy_read_timeout 120s;
    }
}

# HTTPS - only active after certs are generated
# To enable: run init-ssl.sh then uncomment this block and restart nginx
# server {
#     listen 443 ssl;
#     server_name api.manakrishi.com;
#     client_max_body_size 10M;
#
#     ssl_certificate /etc/letsencrypt/live/api.manakrishi.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.manakrishi.com/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_prefer_server_ciphers on;
#
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#
#     location /static/ {
#         alias /app/staticfiles/;
#         expires 30d;
#     }
#
#     location / {
#         proxy_pass http://django;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_connect_timeout 30s;
#         proxy_read_timeout 120s;
#     }
# }
